# Dependencies
node_modules/
.pnpm-store/

# Build output
dist/
build/
*.tsbuildinfo
.turbo/
coverage/

# Secrets — never commit (TraceGuard stores secret references only, never secrets)
.env
.env.*
!.env.example

# Editor / OS
.DS_Store
*.log

# Windows / WSL download artifacts
*:Zone.Identifier
