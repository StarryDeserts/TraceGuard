# Dependencies
node_modules/
.pnpm-store/

# Build output
dist/
build/
*.tsbuildinfo
.turbo/
coverage/

# Secrets — never commit (TraceGuard stores secret references only, never secrets)
.env
.env.*
!.env.example

# Editor / OS
.DS_Store
*.log

# Windows / WSL download artifacts
*:Zone.Identifier

# CodeGraph local index (machine-local, rebuilt on demand)
.codegraph/

# Claude Code session continuation dumps
*-this-session-is-being-continued-from-a-previous-c.txt
